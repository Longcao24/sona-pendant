# --- models: too big for git (100 MB file limit). Download from the release:
# https://github.com/Longcao24/sona-pendant/releases/download/v1.0.0/nuna_production_model_with_weighted_loss_16_20.zip
# unzip at repo root, then `./server/demo.sh` works.
nuna_production_model/
nuna_production_model.zip
nuna_production_model_with_weighted_loss_16_20/
server/deploy_hf/model/

# python
server/.venv/
__pycache__/
*.pyc

# node / expo
app/node_modules/
app/.expo/
app/dist/

# android build output
app/android/build/
app/android/app/build/
app/android/.gradle/
app/android/local.properties

# artifacts
*.apk
*.hex
*.uf2
*.zip

# os / editor
.DS_Store
.idea/
.vscode/
.claude/

# release signing — NEVER commit (public repo)
app/android/keystore.properties
app/android/app/sona-release.keystore
*.keystore
